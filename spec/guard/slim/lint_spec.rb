require 'spec_helper'

describe Guard::Slim::Lint do
  it 'has a version number' do
    expect(Guard::Slim::Lint::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
